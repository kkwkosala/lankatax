// Placeholder environment - real values live in environment.local.ts (gitignored).
// See .env.example for setup instructions.
export const environment = {
  production: false,
  supabaseUrl: '',
  supabaseAnonKey: '',
};